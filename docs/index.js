function out(name) {
    let p = document.getElementById('out');
    p.innerHTML = "Любопытство — не порок, а источник знаний!";
  return "<h1>My name is " + `${name}` + " and this is my homework.</h1>";
  }
  
